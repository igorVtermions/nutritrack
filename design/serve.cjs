const http=require('http'),fs=require('fs'),path=require('path');
http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');const p=path.resolve(__dirname,'.'+decodeURIComponent(url.pathname==='/'?'/index.html':url.pathname));if(!p.startsWith(__dirname+path.sep)){res.writeHead(403);return res.end();}fs.readFile(p,(err,data)=>{if(err){res.writeHead(404);return res.end('Not found');}res.setHeader('Content-Type',p.endsWith('.svg')?'image/svg+xml':p.endsWith('.html')?'text/html; charset=utf-8':'application/json');res.end(data);});}).listen(4173,'127.0.0.1',()=>console.log('Design review ready at http://127.0.0.1:4173'));
